# Use DecapBridge PKCE auth (required)
backend:
  name: git-gateway
  repo: mgts25/newavi
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/24f9e953-bb44-4b1d-bd9f-f4b7f6c53dd3/pkce
  auth_token_endpoint: /sites/24f9e953-bb44-4b1d-bd9f-f4b7f6c53dd3/token
  gateway_url: https://gateway.decapbridge.com

  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Enable PKCE fields (optional, recommended)
auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Add site links in Decap CMS (optional)
site_url: https://cosmic-dragon-248062.netlify.app

# Editors sign in via DecapBridge (Google); every change they save here is
# committed straight to the GitHub repo, which triggers a new Netlify build.
media_folder: "src/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "games"
    label: "Game Library"
    folder: "src/_data/games"
    create: true
    delete: true
    slug: "{{slug}}"
    identifier_field: "title"
    summary: "{{title}} — {{genre}}"
    sortable_fields: ["order", "title", "genre"]
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Genre", name: "genre", widget: "string" }
      - { label: "Price Label", name: "price", widget: "string", default: "Free", hint: "Shown under the game tile, e.g. Free, $2.99" }
      - { label: "Cover Image", name: "image", widget: "image" }
      - label: "Icon Badge"
        name: "icon"
        widget: "string"
        hint: "A Material Symbols icon name, e.g. rocket_launch, swords, speed. Browse names at fonts.google.com/icons"
      - label: "Accent Color"
        name: "accent"
        widget: "select"
        default: "primary"
        options:
          - { label: "Green", value: "primary" }
          - { label: "Light Green", value: "primary-container" }
          - { label: "Blue", value: "tertiary" }
          - { label: "Light Blue", value: "tertiary-container" }
          - { label: "Gray", value: "secondary" }
      - { label: "Hover Preview Video (optional)", name: "video", widget: "file", required: false, hint: "Upload a short .mp4 (a few seconds, ideally under ~8MB) — it plays when a visitor hovers the tile" }
      - { label: "Sort Order", name: "order", widget: "number", default: 0, hint: "Lower numbers appear first in the grid" }

  - name: "pricing"
    label: "Pricing Page"
    files:
      - name: "pricing"
        label: "Pricing Tiers & FAQ"
        file:
